# Variáveis de Ambiente para o Backend

# Segredo para assinatura de tokens JWT (OBRIGATÓRIO)
# Use um valor longo, aleatório e seguro em produção
JWT_SECRET="aVerySecureAndRandomSecretKeyForJWTGeneration_ChangeThis!"

# Porta em que o servidor Express vai rodar (Opcional, padrão 3000 no server.js)
PORT=3000

# Caminho para o arquivo do banco de dados SQLite (Opcional, padrão inventory_data.db na raiz do backend)
SQLITE_PATH="./inventory_data.db"